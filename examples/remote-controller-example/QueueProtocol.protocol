typestate QueueProtocol {

  EMPTY = {
    void enqueue(Robot): NON_EMPTY,
    drop: end
  }

  NON_EMPTY = {
     void enqueue(Robot): NON_EMPTY,
     Robot dequeue(): ENQUEUE_SAME_ELEMENT,
     void turnAllOff(): EMPTY
  }

  ENQUEUE_SAME_ELEMENT = {
    void enqueue(Robot): NON_EMPTY
  }

}
