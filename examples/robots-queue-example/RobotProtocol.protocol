typestate RobotProtocol {
  OFF = {
    void turnOn(): IDLE,
    drop: end
  }

  IDLE = {
    void turnOff(): OFF,
    boolean move(double, double): <true: IDLE, false: RECHARGE>
  }

  RECHARGE = {
    void turnOff(): OFF,
    void recharge(): IDLE
  }
}
