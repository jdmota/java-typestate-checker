package demos.collection.StackUser2;

import demos.collection.Stack;

typestate StackUserProtocol {
	Init = {
		void produce(int) : Consume,
		void produce() : Consume,
		void close() : end
	}

	Consume = {
		void produce(int) : Consume,
		void produce() : Consume,
		void consume() : Init
	}
}
